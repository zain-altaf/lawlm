# Airflow dependencies for Legal Document Pipeline
# Install with: pip install -r requirements-airflow.txt

# Core Airflow with all necessary extras
apache-airflow[postgres,celery]==2.7.3

# Flask limiter (missing dependency)
Flask-Limiter==3.5.0

# Additional providers for enhanced functionality
apache-airflow-providers-postgres==5.7.1

# Backoff library for API retry logic
backoff==2.2.1

# Additional utilities
psycopg2-binary==2.9.7  # PostgreSQL adapter
sqlalchemy==1.4.53  # Required for Airflow

# Ensure compatible versions
werkzeug==2.3.7
flask==2.3.3