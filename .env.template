# Legal Document Processing Pipeline Environment Configuration

# ============================================================================
# Court Listener API Configuration
# ============================================================================
# Required: API key for fetching legal documents from CourtListener
# Get your key at: https://www.courtlistener.com/api/
CASELAW_API_KEY=your_courtlistener_api_key_here

# ============================================================================
# Qdrant Vector Database Configuration
# ============================================================================

# Option 1: Local Qdrant (Default)
# For local development and testing
# QDRANT_URL=http://localhost:6333

# Option 2: Docker Compose Setup
# When running with docker-compose, use the service name
QDRANT_URL=http://qdrant:6333

# Option 3: Qdrant Cloud (Recommended for Production)
# Get your cluster URL and API key from: https://cloud.qdrant.io/
# Free tier provides 1GB storage permanently
# QDRANT_URL=https://your-cluster-id.us-east-1-0.aws.cloud.qdrant.io:6333
QDRANT_API_KEY=your_qdrant_api_key_here
QDRANT_CLUSTER_NAME=your_cluster_name

# ============================================================================
# Optional Configuration
# ============================================================================

# Qdrant timeout settings (seconds)
# QDRANT_TIMEOUT=30

# Processing settings
# WORKING_DIRECTORY=data
# LOG_LEVEL=INFO

# Model settings (if you want to override defaults)
# EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
# CHUNKING_MODEL=nlpaueb/legal-bert-base-uncased
# TARGET_CHUNK_SIZE=384

# ============================================================================
# Usage Notes
# ============================================================================
# 
# 1. Copy this file to '.env' and fill in your credentials
# 2. Never commit the actual .env file with credentials to version control
# 3. For Qdrant Cloud setup:
#    a. Sign up at https://cloud.qdrant.io/
#    b. Create a free cluster (1GB storage)
#    c. Get your cluster URL and API key
#    d. Set QDRANT_URL and QDRANT_API_KEY above
# 4. The free tier supports approximately 500K vectors with metadata
# 5. Auto-suspension occurs after 1 week of inactivity (cloud only)